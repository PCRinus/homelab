services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    container_name: uptime-kuma
    network_mode: host
    volumes:
      - /home/mircea/docker/uptime-kuma:/app/data
      - /run/user/1000/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
