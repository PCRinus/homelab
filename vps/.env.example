PANGOLIN_CONFIG_DIR=/opt/pangolin/config
DOZZLE_HOSTNAME=homeserver-vps

# Keep this private. Prefer the VPS Tailscale IPv4 address once Tailscale is installed.
# Use 127.0.0.1 until the remote agent transport is ready.
DOZZLE_AGENT_BIND=127.0.0.1
